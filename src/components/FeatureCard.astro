---
const { title, desc, icon = "✦" } = Astro.props;
---

<div class="p-5 rounded-[var(--nw-radius-md)] border border-white/10 bg-white/5">
  <div class="text-2xl">{icon}</div>
  <h3 class="mt-3 text-lg font-semibold">{title}</h3>
  <p class="mt-2 text-white/75">{desc}</p>
</div>
