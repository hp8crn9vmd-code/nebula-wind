---
const { title = "Ready to build?", subtitle = "NebulaWind is clean-room and production-first.", href = "/docs/" } = Astro.props;
---

<section class="mt-10 p-6 rounded-[var(--nw-radius-md)] border border-white/10 bg-white/5">
  <div class="flex flex-col md:flex-row md:items-center md:justify-between gap-4">
    <div>
      <h2 class="text-xl font-semibold">{title}</h2>
      <p class="mt-2 text-white/75">{subtitle}</p>
    </div>
    <a class="inline-flex justify-center px-4 py-2 rounded-[var(--nw-radius-md)] border border-white/10 bg-white/10 hover:bg-white/15 transition"
       href={href}>
      Explore →
    </a>
  </div>
</section>
